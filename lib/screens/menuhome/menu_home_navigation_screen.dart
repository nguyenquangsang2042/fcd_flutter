import 'package:flutter/material.dart';

class MenuHomeNavigationScreen extends StatelessWidget {
  const MenuHomeNavigationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
