import 'package:flutter/cupertino.dart';

class FlightScheduleMDScreen extends StatelessWidget {
  const FlightScheduleMDScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
